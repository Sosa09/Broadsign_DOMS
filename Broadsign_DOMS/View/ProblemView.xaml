<UserControl x:Class="Broadsign_DOMS.View.ProblemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Broadsign_DOMS.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <!--ssh remote functionalities-->
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="Button" x:Key="BtnCommands">
                    <Setter Property="Command" Value="{Binding RemoteOptionsCommand}"/>
                </Style>
            </StackPanel.Resources>

            <Button Content="Connect"           Command="{Binding ConnectSshCommand}"/>
            <Button Content="Disconnect"        Command="{Binding ConnectSshCommand}"/>

            <Button Content="Log Files"         Style="{DynamicResource BtnCommands}" CommandParameter="files"      />        <!--IDEA: get the files and let the user read them in the tool or download them-->
            <Button Content="Xrandr Get"        Style="{DynamicResource BtnCommands}" CommandParameter="xrandr get" />
            <Button Content="Xrandr Set"        Style="{DynamicResource BtnCommands}" CommandParameter="xrandr set" />
            <Button Content="Ad Copies Files"   Style="{DynamicResource BtnCommands}" CommandParameter="documents"  />    <!--IDEA: get the ad copies by display unit and show the names--> 
            <Button Content="Get Process"       Style="{DynamicResource BtnCommands}" CommandParameter="process"    />
            <Button Content="Consul update"     Style="{DynamicResource BtnCommands}" CommandParameter="consul"     />
            <Button Content="Reboot"            Style="{DynamicResource BtnCommands}" CommandParameter="reboot"     />
            
        </StackPanel>
        <!--active sessions-->
        <StackPanel VerticalAlignment="Bottom" >
            <DataGrid ItemsSource="{Binding ActiveSessions}" SelectedItem="{Binding SelectedSession}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding HostName}"/>
                    <DataGridTextColumn Header="Alive" Binding="{Binding IsConnected}"/>
                    <DataGridTextColumn Header="Vnc" Binding="{Binding VncPort}"/>
                </DataGrid.Columns>
            </DataGrid>
            <local:StartView Height="100"/>
        </StackPanel>
        <!--player list, filter, output-->
        <StackPanel Grid.Column="2">
            <DockPanel LastChildFill="False">
                <TextBox Margin="10" Width="200" Text="{Binding HostName, UpdateSourceTrigger=PropertyChanged}"/>
                <ComboBox ItemsSource="{Binding DomainList}" SelectedItem="{Binding SelectedDomain}"></ComboBox>

            </DockPanel>
            <DataGrid ItemsSource="{Binding PlayerList}" SelectedItem="{Binding SelectedPlayer}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Text="{Binding SelectedSession.HostName}" Margin="20"/>
            <StackPanel Visibility="{Binding LogFileGridVisibility}">
                <DataGrid   ItemsSource="{Binding LogFiles}" SelectionMode="Extended" SelectedItem="{Binding SelectedFile}" AutoGenerateColumns="False">
                    <DataGridTextColumn Header="File Name"></DataGridTextColumn>
                </DataGrid>
                <Button Content="Download Files"/>
            </StackPanel>
            <TextBlock Text="{Binding Result}"></TextBlock>
        </StackPanel>

    </Grid>
</UserControl>
