<UserControl x:Class="Broadsign_DOMS.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Broadsign_DOMS.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
      
        <LinearGradientBrush x:Key="GradientBrushBG" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="black" Offset="0" />
            <GradientStop Color="#353535" Offset="5" />
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="GradientBrushGB" StartPoint="1,1" EndPoint="0,0">
            <GradientStop Color="black" Offset="0" />
            <GradientStop Color="#353535" Offset="5" />
        </LinearGradientBrush>

    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Button Content="Troubleshooting" FontSize="48" Command="{Binding ProblemView}" Background="{StaticResource GradientBrushBG}" Foreground="white"></Button>
        <Button Grid.Row="1" Content="Administration" FontSize="48" Command="{Binding AdminView}" Background="{StaticResource GradientBrushGB}" Foreground="white"></Button>
    </Grid>
</UserControl>
