<UserControl x:Class="Broadsign_DOMS.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Broadsign_DOMS.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Bottom">
            <DockPanel LastChildFill="False" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <Label Content="Username:       "/>
                <TextBox Text="{Binding Username}" Width="300"/>
            </DockPanel>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,5,0,0">
            <Button Content="Login" HorizontalAlignment="Center" Width="390" Height="50" Command="{Binding LoginButtonCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
